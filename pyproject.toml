[project]
name = "dnp_frontend"
version = "0.1.0"
description = "Streamlit client for the phonebook app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy-protobuf>=3.6.0",
    "streamlit>=1.44.1",
]
